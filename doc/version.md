## 版本 (Version)

```
GET /api/version
```

检索 Ollama 版本

### 示例 (Examples)

#### 请求 (Request)

```shell
curl http://localhost:11434/api/version
```

#### 响应 (Response)

```json
{
  "version": "0.5.1"
}
```
